apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: "2025-07-16T09:07:45Z"
  generation: 1
  name: gitrepositories.azuredevops.kog.krateo.io
  resourceVersion: "112491"
  uid: f6e5d6ac-221a-4872-87d1-5527d5c1220f
spec:
  conversion:
    strategy: None
  group: azuredevops.kog.krateo.io
  names:
    categories:
    - gitrepository
    - restresources
    - rr
    kind: GitRepository
    listKind: GitRepositoryList
    plural: gitrepositories
    singular: gitrepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          spec:
            properties:
              Authorization:
                description: 'PARAMETER: header, VERB: Post - Basic Auth header (Basic
                  <base64-encoded-username:password>)'
                type: string
              api-version:
                description: 'PARAMETER: query, VERB: Post - API version (e.g., 7.2-preview.2)'
                type: string
              authenticationRefs:
                description: AuthenticationRefs represent the reference to a CR containing
                  the authentication information. One authentication method must be
                  set.
                properties:
                  basicAuthRef:
                    type: string
                type: object
              defaultBranch:
                type: string
              id:
                description: 'PARAMETER: path, VERB: Get - The name or ID of the repository.'
                type: string
              initialize:
                description: Indicates if the repository should be initialized with
                  an initial commit
                type: boolean
              name:
                type: string
              organization:
                description: 'PARAMETER: path, VERB: Post - Organization name'
                type: string
              parentRepository:
                properties:
                  id:
                    type: string
                  project:
                    properties:
                      id:
                        type: string
                    type: object
                type: object
              project:
                properties:
                  id:
                    type: string
                type: object
              projectId:
                description: 'PARAMETER: path, VERB: Post - Project ID or name'
                type: string
              remoteUrl:
                description: 'IDENTIFIER: remoteUrl'
                type: string
              sourceRef:
                description: 'PARAMETER: query, VERB: Post - Specify the source refs
                  to use while creating a fork repo'
                type: string
              sshUrl:
                description: 'IDENTIFIER: sshUrl'
                type: string
              url:
                description: 'IDENTIFIER: url'
                type: string
            required:
            - authenticationRefs
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedObjectRef:
                properties:
                  apiVersion:
                    description: API version of the object.
                    type: string
                  kind:
                    description: Kind of the object.
                    type: string
                  name:
                    description: Name of the object.
                    type: string
                  namespace:
                    description: Namespace of the object.
                    type: string
                type: object
              id:
                type: string
              name:
                type: string
              remoteUrl:
                type: string
              sshUrl:
                type: string
              url:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    categories:
    - gitrepository
    - restresources
    - rr
    kind: GitRepository
    listKind: GitRepositoryList
    plural: gitrepositories
    singular: gitrepository
  conditions:
  - lastTransitionTime: "2025-07-16T09:07:45Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2025-07-16T09:07:45Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
